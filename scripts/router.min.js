
var Case = Barba.BaseView.extend({
    namespace: "page-case",
    onEnterCompleted: function() {
        $(".p-case").imagesLoaded(function() {
            $(".s-grid").length && ($("body").hasClass("d-mobile") || sGrid()), $(".s-image").length && ($("body").hasClass("d-mobile") || sImage()), $(".s-cinegraph").length && sCinegraph(), $(".s-dragger").length && ($("body").hasClass("d-mobile") || sDragger()), $(".s-split").length && ($("body").hasClass("d-mobile") || sSplit()), $(".s-tiles").length && ($("body").hasClass("d-mobile") || sTiles()), $(".s-video").length && ($("body").hasClass("d-mobile") || sVideo()), $(".s-prev").length && sPrevCase(), $(".s-next").length && sNextCase(), $(".s-prev-category").length && sPrevCategory(), $(".s-next-category").length && sNextCategory(), $("body").hasClass("d-mobile") && mCase()
        })
    }
});
Case.init();

var Video = Barba.BaseView.extend({
    namespace: "page-video",
    onEnterCompleted: function() {
        $(".p-video").imagesLoaded(function() {
            pVideo(), sSidenav(), initPage(), $("html").removeAttr("class")
        })
    }
});
Video.init(), Barba.Pjax.start(), Barba.Pjax.getTransition = function() {
    return $("html").hasClass("a-loader") ? LoaderTransition : $("html").hasClass("a-work-case-mobile") ? WorkCaseMobile : HideShowTransition
};